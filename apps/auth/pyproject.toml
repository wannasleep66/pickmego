[project]
name = "auth"
version = "0.1.0"
description = "Authentication service"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "pydantic-settings>=2.14.2",
]
