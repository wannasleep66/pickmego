[project]
name = "pickmego"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mypy>=2.3.0",
    "pre-commit>=4.6.1",
    "ruff>=0.16.0",
]

[tool.uv.workspace]
members = [
    "apps/*",
    "packages/*",
]

[tool.mypy]
plugins = [
    "pydantic.mypy"
]
